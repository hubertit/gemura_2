/**
 * Feature flags for Orora web.
 * Orora is farm-level (no milk collection at MCC); collections and suppliers can be re-enabled later.
 */

/** When false, hide Collections nav, dashboard tab/cards/charts, and quick action. Routes and API remain. */
export const showCollections = false;

/** When false, hide Suppliers nav, dashboard card, and quick action. Routes and API remain. */
export const showSuppliers = false;

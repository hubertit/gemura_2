# Orora Mobile

Placeholder for the **Orora Flutter mobile app** (future).

This directory is reserved for the Orora cattle-farming mobile application. It will follow the same structure as `apps/gemura-mobile` (e.g. `lib/core/`, `lib/features/`, `lib/shared/`) and use **snake_case** for feature folder names per Dart conventions.

Until the app is started, keep this README so the placeholder purpose is clear.
